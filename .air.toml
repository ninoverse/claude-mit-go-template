root = "."
tmp_dir = "tmp"

[build]
  # Point this at your binary's package, e.g. ./cmd/app.
  cmd = "go build -o ./tmp/main ./cmd/app"
  bin = "./tmp/main"
  include_ext = ["go"]
  exclude_dir = ["tmp", "bin", "dist", "testdata"]
  delay = 1000

[misc]
  clean_on_exit = true
