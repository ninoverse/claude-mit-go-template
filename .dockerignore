# VCS & CI
.git
.github

# Agent config & project docs
.agents
.claude
*.md

# The agentcfg binary `make agentcfg` fetches. Gitignored, so it exists only
# on a working laptop — which is exactly where `docker build` runs.
.agentcfg

# Build / test artifacts
bin/
dist/
tmp/
coverage.txt
*.test
*.out

# Unused Go workspace files
go.work
go.work.sum

# Files not needed inside the image
Dockerfile
.dockerignore
.air.toml
