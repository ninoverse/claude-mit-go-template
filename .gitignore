# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
/bin/
/dist/

# Test & coverage output
*.test
*.out
coverage.txt

# Go workspace file (this template is a single module — no go.work)
go.work
go.work.sum

# air live-reload working dir
/tmp/

# Editor / OS
.DS_Store

# The agentcfg binary `make agentcfg` fetches. Downloaded per pin, never
# committed — Renovate collects post-upgrade changes from `git status`, so an
# unignored copy would land three megabytes in a pull request about prose.
/.agentcfg/
